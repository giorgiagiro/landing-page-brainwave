import React from "react";

const Benefits = () => {
    return (
        <section id="features">
            <div className="container relative z-2">
                <Heading />
            </div>
        </section>
    );
};

export default Benefits;
